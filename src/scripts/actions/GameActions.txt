В функции _complete находится завершение уровня
В нем вызывался экран

Чтобы вызвать экран:

```
    this._scene.scene.start('Menu')
    Settings.setScreen(screen.COMPLETE)
```

В функции _rating находится вызов модалки с котнентом рейтингов